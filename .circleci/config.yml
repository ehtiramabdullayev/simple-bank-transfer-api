version: 2.1

orbs:
  maven: circleci/maven@1.0.0

workflows:
  maven_test:
    jobs:
      - maven/test:
         context: SonarCloud
      - run:
            name: Analyze on SonarCloud
            command: mvn verify sonar:sonar

